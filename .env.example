# Database Configuration
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
# For production with docker-compose, use db as the hostname:
DATABASE_URL=postgresql://postgres:postgres@db:5432/obview
POSTGRES_USER=postgres
# For production, generate a strong password: openssl rand -base64 24
POSTGRES_PASSWORD=postgres
POSTGRES_DB=obview

# Session Configuration
# Generate a secure random string: openssl rand -base64 32
SESSION_SECRET=your_secure_session_secret_here

# Email Configuration (SendGrid)
# Obtain from: https://app.sendgrid.com/settings/api_keys
# Required for email notifications and invitations
SENDGRID_API_KEY=your_sendgrid_api_key_here

# Initial Admin User (used only on first launch)
# These credentials will be used to create the initial administrator account
ADMIN_USERNAME=admin
# Use a strong password with at least 8 characters, including numbers and special characters
ADMIN_PASSWORD=change_me_immediately
ADMIN_NAME=Administrator
# Use a real email address where you want to receive system notifications
ADMIN_EMAIL=admin@yourdomain.com

# Application Configuration
# Set this to your full domain name in production (e.g., https://obview.io)
# Required for generating correct invitation links
APP_URL=http://localhost:3000

# Uncomment and set these additional configuration options as needed:
# Maximum upload file size in bytes (default: 500MB)
# MAX_FILE_SIZE=524288000

# File storage path (default: uploads)
# UPLOAD_PATH=uploads