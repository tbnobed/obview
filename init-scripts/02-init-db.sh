#!/bin/bash
# This script is intended to run database migrations
# However, since migrations are handled by the init-db.sql script, this is no longer needed
echo "Migrations already applied via SQL scripts"
exit 0